# E17 Acceptance

- GET /tag/_metrics returns text in Prometheus exposition format (ends with newline).
- Smoke script runs and prints first lines of metrics.
- SLO docs present: docs/ops/tag_slo.md.
