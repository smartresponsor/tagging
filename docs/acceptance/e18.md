# E18 Acceptance

- Run smoke twice: second run should show >0 cache hits.
- Force many read requests with same actor to hit 429 after burst limit.
- Write operation invalidates caches (verify by hit/miss counters).
