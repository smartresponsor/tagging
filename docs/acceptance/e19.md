# E19 Acceptance
1) Subscribe a local receiver (ngrok or test endpoint) via POST /tag/_webhooks/subscribe.
2) POST /tag/_webhooks/test → receiver gets one event; verify HMAC header.
3) Open report/tag/audit.ndjson → new event line appears.
4) Metrics show increments for delivered/failed webhooks.
