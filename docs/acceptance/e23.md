# E23 Acceptance

- GET /tag?limit=1 with no roles → 200 (read allowed).
- POST /tag with no roles and fallback_allow_all=false → 403 forbidden.
- POST /tag with X-Roles: tag.editor → 200.
- POST /tag/_webhooks/test requires tag.owner when path_overrides maps to admin.
