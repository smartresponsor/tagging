# E24 Acceptance
1) Подпишитесь на несуществующий URL; вызовите test — job появляется в spool; worker ретраит и уходит в DLQ.
2) Подпишитесь на реальный URL; test — доставлено за 1 попытку; retried_total не растёт.
3) Проверьте, что счётчики retried_total/dlq_total инкрементируются корректно.
